# Used directly by docker-compose.yml to configure the Postgres container.
# Copy to .env and keep these values in sync with backend/.env
POSTGRES_USER=cpms_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=cpms_db
